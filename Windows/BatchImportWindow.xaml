<ctrl:FrostyDockableWindow x:Class="MeshSetExtender.Windows.BatchImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        xmlns:core="clr-namespace:Frosty.Core.Controls;assembly=FrostyCore"
        mc:Ignorable="d"
        Title="Batch Import (Auto LOD)" Height="380" Width="800" 
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="FrostyDataGridStyle" TargetType="DataGrid">
                <Setter Property="Background" Value="{StaticResource ListBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
                <Setter Property="BorderBrush" Value="{StaticResource ControlBackground}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="RowBackground" Value="{StaticResource ListBackground}"/>
                <Setter Property="AlternatingRowBackground" Value="{StaticResource ControlBackground}"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource ControlBackground}"/>
                <Setter Property="VerticalGridLinesBrush" Value="{StaticResource ControlBackground}"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="CanUserAddRows" Value="False"/>
                <Setter Property="CanUserDeleteRows" Value="False"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
            </Style>
            
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="{StaticResource ControlBackground}"/>
                <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
                <Setter Property="Padding" Value="5,3"/>
                <Setter Property="BorderBrush" Value="{StaticResource ControlBackground}"/>
                <Setter Property="BorderThickness" Value="0,0,1,1"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
            
            <!-- Selection / hover brushes are NOT exposed by FrostyControls' Generic.xaml.
                 Using StaticResource for HighlightBackground / HighlightFontColor / HoverBackground
                 silently resolves to UnsetValue, which crashes DataGridCell.SyncIsSelected.
                 Hardcode literal brushes instead. -->
            <SolidColorBrush x:Key="GridHighlightBackground" Color="#FF353535"/>
            <SolidColorBrush x:Key="GridHoverBackground"     Color="#FF2F2F2F"/>

            <Style TargetType="DataGridCell">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource FontColor}"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="4,2"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource GridHighlightBackground}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style TargetType="DataGridRow">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsSelected" Value="True">
                        <Setter Property="Background" Value="{StaticResource GridHighlightBackground}"/>
                    </Trigger>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource GridHoverBackground}"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <Style TargetType="CheckBox" BasedOn="{StaticResource {x:Type CheckBox}}"/>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Select meshes to import and specify FBX files:" 
                   FontWeight="SemiBold" Foreground="{StaticResource FontColor}" 
                   Margin="10,10,10,5" VerticalAlignment="Center"/>

        <DataGrid Grid.Row="1" x:Name="MeshGrid" Margin="10,0,10,10"
                  Style="{StaticResource FrostyDataGridStyle}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Import" Binding="{Binding Enabled, UpdateSourceTrigger=PropertyChanged}" Width="50"/>
                <DataGridTextColumn Header="Asset Name" Binding="{Binding AssetName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="LODs" Binding="{Binding LodCount}" Width="50" IsReadOnly="True"/>
                <DataGridTemplateColumn Header="FBX File" Width="*" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding FbxFileName}"
                                           VerticalAlignment="Center" Margin="4,0"
                                           TextTrimming="CharacterEllipsis"
                                           ToolTip="{Binding FbxFullPath}"/>
                                <Button Grid.Column="1" Content="Browse..." Width="70" Margin="2,1"
                                        Click="BrowseFbx_Click" Tag="{Binding}"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10,0,10,10">
            <CheckBox x:Name="SelectAllCheckBox" Content=" Select All" 
                      Checked="SelectAll_Checked" Unchecked="SelectAll_Unchecked"
                      VerticalAlignment="Center" Margin="0,0,20,0" Foreground="White"/>
            <TextBlock x:Name="StatusText" VerticalAlignment="Center" Opacity="0.7"
                       Foreground="{StaticResource FontColor}"/>
        </StackPanel>

        <Border Grid.Row="3" Background="{StaticResource WindowBackground}">
            <Grid Margin="10,5">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Auto-Match by Name" Width="130" Margin="0,0,10,0" 
                            Click="AutoMatch_Click" Height="28"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Import Selected" Width="110" Margin="0,0,10,0" 
                            Click="Import_Click" IsDefault="True"/>
                    <Button Content="Cancel" Width="80" IsCancel="True" Click="Cancel_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
