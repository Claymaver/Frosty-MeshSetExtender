<ctrl:FrostyDockableWindow x:Class="MeshSetExtender.Windows.ResourceBrowserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ctrl="clr-namespace:Frosty.Controls;assembly=FrostyControls"
        mc:Ignorable="d"
        Title="Select Resource" Height="500" Width="600" 
        ResizeMode="NoResize" 
        WindowStartupLocation="CenterOwner"
        FrostyLoaded="FrostyDockableWindow_FrostyLoaded">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/FrostyControls;component/Themes/Generic.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="38"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <DockPanel Grid.Row="0" Margin="10" LastChildFill="True">
            <Label Content="Search:" Width="60" Foreground="{StaticResource FontColor}" FontFamily="Global User Interface"/>
            <ctrl:FrostyWatermarkTextBox x:Name="SearchTextBox" Padding="4" FontFamily="Global User Interface" BorderThickness="1" WatermarkText="Type to filter..." TextChanged="SearchTextBox_TextChanged"/>
        </DockPanel>

        <!-- Resource List -->
        <Border Grid.Row="1" Margin="10,0,10,10" BorderBrush="{StaticResource ControlBackground}" BorderThickness="1">
            <ListBox x:Name="ResourceListBox" Background="{StaticResource ListBackground}" Foreground="{StaticResource FontColor}" MouseDoubleClick="ResourceListBox_MouseDoubleClick">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" Margin="2">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource FontColor}"/>
                            <TextBlock Text="{Binding TypeDisplay}" FontSize="10" Foreground="{StaticResource FontColor}" Opacity="0.6"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Bottom Button Bar -->
        <Border Grid.Row="2" Background="{StaticResource WindowBackground}">
            <Grid Margin="8">
                <Button x:Name="cancelButton" Content="Cancel" HorizontalAlignment="Left" Width="75" Click="CancelButton_Click"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="selectButton" Content="Select" Width="75" Click="SelectButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ctrl:FrostyDockableWindow>
